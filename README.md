# Cálculo Potencias (BACK)

En este proyecto llamamos realizamos los cálculos y los devolvemos en JSON.

## Para arrancarlo

Se compila con `tsc` y se arranca con `node dist/index` o `nodemon dist/index`